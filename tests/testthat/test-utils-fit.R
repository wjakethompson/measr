test_that("install_backend", {
  # pkg <- "cmdstanr"
  # expect_null(install_backend(pkg))

})

# new_dina <- dplyr::slice_sample(dina_data, n=100)
# dina_mcmc_utils_fit <- measr_dcm(data = new_dina, missing = NA, qmatrix = q_matrix,
#                                               resp_id = "resp_id", item_id = "item",
#                                               type = "dina", method = "mcmc",
#                                               cores = 1, chains = 1, backend = "cmdstanr")
